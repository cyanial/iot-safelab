import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


// setInterval(() => {
//   getDevMesg();
// }, 3000)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
